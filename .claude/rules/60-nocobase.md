# 60 · NocoBase (pulk-side, 참고용)

ACR은 NocoBase를 직접 다루지 않는다. pulk 연계 시 참고만.

- NocoBase는 pulk의 Shell. core/영구 brain 아님.
- 타임스탬프 `createdAt`(camelCase). `created_at` 정렬 = 빈배열 버그.
- pulk 연동 상세는 pulk repo `docs/index/db.md` 참조.
